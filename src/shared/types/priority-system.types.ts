/**
 * Represents the priority levels used in the system for sorting or highlighting items.
 *
 * - `"LOW"`: Lowest priority, typically non-urgent tasks or notifications.
 * - `"MEDIUM"`: Moderate priority, should be handled soon but not immediately.
 * - `"IMPORTANT"`: High priority, needs attention before other non-urgent items.
 * - `"URGENT"`: Highest priority, requires immediate action.
 */
export type TPrioritySystem = "LOW" | "MEDIUM" | "IMPORTANT" | "URGENT";
